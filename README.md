# Todo List API - FlyRank Week 2 Assignment

A simple CRUD API built with Node.js and Express for FlyRank Week 2.

## How to Run

1. Install dependencies:
   npm install

2. Start the server:
   node index.js

The server will run on http://localhost:3000.

Just clone this repo and try it out.

## API Documentation

You can test all endpoints visually using Swagger UI:
* Swagger UI: http://localhost:3000/docs

## Available Endpoints

* GET / - API Info
* GET /health - Health Check
* GET /tasks - List all tasks (supports filtering via `?done=true` or `?done=false`)
* GET /tasks/:id - Get a task by ID
* POST /tasks - Create a task
* PUT /tasks/:id - Update a task
* DELETE /tasks/:id - Delete a task

**Here's the project executed:**
<img width="1589" height="913" alt="image" src="https://github.com/user-attachments/assets/5edfa5bc-6a12-4f95-9492-c987d5644dea" />

 
---

## Ai usage

I used AI as a teacher, not the all knowing god. I've used it for:

* **Coaching:** Since this is my first time using Node.js, I used AI to coach me on syntax and structure. I did not blindly ask it to make the tasks for me; I wanted to learn as much as possible.
* **Debugging and understanding errors:** It helped me understand backend logic and explain why errors happened instead of just giving me quick patches.
* **Creating the Swagger UI settings file (openapi.json):** Used it to draft the structure of the API documentation.
* **Making this README:** Formatting and refining the documentation.

---

## AI vs Me (Stage 7 - Rematch)

### 1. The Prompt I Used
"make a simple node.js project that includes a simples todo list api for every CRUD operation, considering it must have a swagger UI dashboard in the end and that it must use express. Make it the most simple possible on a single file"

### 2. What did the AI do better?
* **Comments and structure:** The code generated by the AI in `server.js` is very clean, organized, and has descriptive comments for every single part, which makes it easy to read.
* **NPM Scripts:** The AI project used the standard `npm start` script to run the server. In my hand-built version, I was running it directly with `node index.js`. Using `npm start` seems to be much better and more professional because it abstracts the entry file, allowing anyone to run the project without needing to know the exact name of the main file.
* **No extra files:** It integrated Swagger directly into the javascript code using JSDoc comments, avoiding the need to manage a separate JSON file.
**Here's the UI AI created:**
  <img width="1862" height="975" alt="image" src="https://github.com/user-attachments/assets/e5803a8e-3668-4e94-80a4-cc221d26aadc" />


### 3. What did it get wrong or ignore from my prompt?
* **Route Names:** Even though I asked for a "todo list api", it named all the endpoints `/todos` instead of `/tasks` (which is what my assignment required).
* **Missing Things:** It quietly decided to run on port 3000 and redirect `/` to the documentation, entirely ignoring the `/health` check and the `?done=true` filter I manually built.

### 4. What my prompt forgot to specify:
I did not specify the exact port, the names of the routes (`/tasks`), or specific endpoints like `/health`. The AI had to guess and make those choices for me.

### 5. One Rematch Change
By changing the prompt to specify the exact port, endpoint paths (/tasks instead of /todos), and the exact list of CRUD methods, the newly regenerated AI code correctly matched my project rules without inventing paths or skipping the health check.


## Conclusion

This assignment was aa good way hands dirty with Node.js, Express, and Swagger, even with a lot of struggles... 

Doing Stages 0 to 6 by hand with a bit of AI help allowed me to start understanding how routing, status codes, and manual validations work for the most part. When it came to Stage 7, I noticed how better the AI does it compared to what I've done.

The biggest lesson is that AI is an amazing junior developer, but it is only as good as the instructions we give it. Learning how to write precise requirements is just as important as writing the code itself, but using AI this way is super effective and helpful.
